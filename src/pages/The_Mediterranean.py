
import streamlit as st
import pandas as pd
import plotly.express as px
import plotly.graph_objects as go

# Page: Explore the Mediterranean
st.header("Deaths at the Mediterranean")
st.markdown("The region with the most recorded deaths is the Mediterranean.")
st.markdown("Include here some lines about the total number of recorded deaths, and the types of deaths that were the most common")
# st.map(df.loc[df['Region of Incident']=='Mediterranean'])

